# Azure FinOps Agent - Production Environment Variables
# Set these in your Azure App Service / Docker container / CI pipeline.
# .NET maps double-underscore (__) to config sections automatically.

# GitHub OAuth App (Production - azure-finops-agent.com)
# Create at: https://github.com/settings/developers
# Callback URL: https://azure-finops-agent.com/auth/github/callback
GitHub__ClientId=your_github_client_id
GitHub__ClientSecret=your_github_client_secret

# Build metadata (set by CI)
BUILD_SHA=dev
BUILD_NUMBER=0
